<forms>
    {% if form_tuples %}
        {% for form_name, form_id in form_tuples %}
            <form url="{{ xform_base_url }}/{{ form_id }}">{{ form_name }}</form>
        {% endfor %}
    {% endif %}
</forms>